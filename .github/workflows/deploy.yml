name: Deploy to Home Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: telcation.f5.si
          username: appuser
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/studentXX/myapp
            sudo -u studentXX git clone https://github.com/${{ github.repository }} . 2>/dev/null || (sudo -u studentXX git fetch && sudo -u studentXX git reset --hard origin/main)
            sudo -u studentXX pip3 install -r requirements.txt --break-system-packages
            sudo pkill -u studentXX -f 'python3 app.py' || true
            sudo -u studentXX nohup python3 /home/studentXX/myapp/app.py > /home/studentXX/myapp/app.log 2>&1 &
